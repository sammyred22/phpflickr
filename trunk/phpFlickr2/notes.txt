self object extends person object
    * flickr.blogs.getList
    * flickr.blogs.postPhoto
    * flickr.contacts.getList
    * flickr.favorites.add
    * flickr.favorites.getList
    * flickr.favorites.remove
    * flickr.groups.pools.getGroups
    * flickr.photos.getContactsPhotos
    * flickr.photos.getCounts
    * flickr.photos.getNotInSet
    * flickr.photos.getUntagged
    * flickr.photosets.create
    * flickr.photosets.orderSets

person object
    * flickr.contacts.getPublicList
    * flickr.favorites.getPublicList
    * flickr.people.findByEmail
    * flickr.people.findByUsername
    * flickr.people.getInfo
    * flickr.people.getPublicGroups
    * flickr.people.getPublicPhotos
    * flickr.photos.getContactsPublicPhotos
    * flickr.photosets.getList
    * flickr.tags.getListUser
    * flickr.tags.getListUserPopular
    * flickr.urls.getUserPhotos
    * flickr.urls.getUserProfile

group object
    * flickr.groups.getInfo
    * flickr.groups.pools.add
    * flickr.groups.pools.getPhotos
    * flickr.groups.pools.remove
    * flickr.urls.getGroup

photo object (create variable to show if taken from set or group or what?)
    * flickr.photos.addTags
    * flickr.photos.getContext
    * flickr.photos.getExif
    * flickr.photos.getInfo
    * flickr.photos.getPerms
    * flickr.photos.getSizes
    * flickr.photos.removeTag
    * flickr.photos.setDates
    * flickr.photos.setMeta
    * flickr.photos.setPerms
    * flickr.photos.setTags
    * flickr.photos.notes.add
    * flickr.photos.notes.delete
    * flickr.photos.notes.edit
    * flickr.photos.transform.rotate
    * flickr.tags.getListPhoto

flickr object
    * flickr.photos.getRecent
    * flickr.photos.search
    * flickr.groups.browse
    * flickr.groups.getActiveList
    * flickr.people.getOnlineList
    * flickr.photos.licenses.getInfo
    * flickr.reflection.getMethods
    * flickr.tags.getRelated
    * flickr.test.echo  (to test initial connection)
    * flickr.test.login  (to test login or auth status)
    * flickr.urls.lookupGroup
    * flickr.urls.lookupUser

photolist object
    * flickr.photosets.getContext
    * flickr.groups.pools.getContext
    * flickr.photos.getContext

photoset object
    * flickr.photosets.addPhoto
    * flickr.photosets.delete
    * flickr.photosets.editMeta
    * flickr.photosets.editPhotos
    * flickr.photosets.getInfo
    * flickr.photosets.getPhotos
    * flickr.photosets.removePhoto

method object
    * flickr.reflection.getMethodInfo

